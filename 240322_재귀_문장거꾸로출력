// Solution 참고

#include <bits/stdc++.h>

using namespace std;

void printReverse(char* str) {
	if (*str == '\0') return;
	else {
		printReverse(str + 1);
		printf("%c", *str);
	}
}

int main() {
	char str[] = "abcdefg";
	printReverse(str);
}

#include <iostream>
#include <string>
#include <vector>

using namespace std;

int main()
{
	int n = 4;
	vector<int> cnt;

	while (n > 0) {
		cnt.push_back(n % 2);
		n /= 2;
	}

	cout << count(cnt.begin(), cnt.end(), 1);

	return 0;
}
